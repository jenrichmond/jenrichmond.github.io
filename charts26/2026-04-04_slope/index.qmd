---
title: "day X"
author: Jen Richmond
date: 2026-04-01
format: html
editor: source
draft: FALSE
code-fold: true
image: featured.png
execute:
  freeze: true
---

```{r}
#| message: false
#| warning: false

library(tidyverse)
library(tidytuesdayR)
library(janitor)
library(ggpubr)
library(Manu)


# choosing a dataset randomly

set.seed(4)
ttyears <- c(2018:2025)
ttweeks <- c(1:52)

# choose a year at random
chosen_year <- sample(ttyears, size = 1)

# choose at week at random
chosen_week <- sample(ttweeks, size = 1)

# read the data from that year/week

df <- tidytuesdayR::tt_load(chosen_year, chosen_week)

# print dataset
print(df)

palm <- df[[1]]


```


The data for this plot comes from a `r chosen_year` TidyTuesday challenge. The Week `r chosen_week` data that year was about Palm Trees. 

![](featured.png)


```{r}
#| fig-show: "hide"

options(scipen = 999)

palm %>%
  filter(climbing != "both") %>%
  ggplot(aes(y = max_stem_height_m, x = max_leaf_number)) +
  geom_jitter() +
  scale_y_continuous(limits = c(0,65), breaks = seq(0,80,20)) +
  scale_x_continuous(limits = c(0,80), breaks = seq(0,80,20)) +
  geom_smooth(method = "lm", se = FALSE, colour = "red")  +
  labs(y = "Maximum height (m)", x = "Maximum leaf number", 
       title = "Taller palm trees have more leaves", 
       caption = "TidyTuesday 2025 Week 11 | Data from palmtrees` R package") +
  theme_pubr() +
  stat_cor(method = "pearson", p.accuracy = 0.001, r.accuracy = 0.01)



```

```{r}
#| include: false
# save plots

ggsave(here::here("charts26", "2026-03-31_challenge", "palm.png"), width = 6, height = 4,  bg = "white")


ggsave("featured.png", width = 6, height = 4,  bg = "white")


```



